<Window x:Class="StatHammer.DesktopClient.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:behaviors="clr-namespace:StatHammer.DesktopClient.Behaviors"
        Title="StatHammer — вход"
        Height="440"
        Width="540"
        MinHeight="440"
        MinWidth="540"
        ResizeMode="CanMinimize"
        WindowStartupLocation="CenterScreen">
    <Grid Background="#F4F4F5">
        <Border Margin="28"
                Padding="24"
                Background="White"
                BorderBrush="#E5E7EB"
                BorderThickness="1"
                CornerRadius="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="StatHammer"
                           FontSize="30"
                           FontWeight="Bold"
                           Foreground="#111827"
                           Margin="0,0,0,8" />

                <TextBlock Grid.Row="1"
                           Text="Войдите в учётную запись"
                           FontSize="16"
                           Foreground="#374151"
                           Margin="0,0,0,20" />

                <StackPanel Grid.Row="2"
                            Margin="0,0,0,12">
                    <TextBlock Text="Email"
                               FontWeight="SemiBold"
                               Margin="0,0,0,5" />

                    <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                             Height="34"
                             Padding="7,5" />
                </StackPanel>

                <StackPanel Grid.Row="3"
                            Margin="0,0,0,12">
                    <TextBlock Text="Пароль"
                               FontWeight="SemiBold"
                               Margin="0,0,0,5" />

                    <PasswordBox Height="34"
                                 Padding="7,5"
                                 behaviors:PasswordBoxAssistant.BindPassword="True"
                                 behaviors:PasswordBoxAssistant.BoundPassword="{Binding Password, Mode=TwoWay}" />
                </StackPanel>

                <TextBlock Grid.Row="4"
                           Text="{Binding ErrorMessage}"
                           Foreground="#B91C1C"
                           TextWrapping="Wrap"
                           Margin="0,0,0,12" />

                <Button Grid.Row="5"
                        Content="Войти"
                        Command="{Binding LoginCommand}"
                        Height="38"
                        Width="130"
                        HorizontalAlignment="Left"
                        FontWeight="SemiBold"
                        Padding="12,5" />

                <TextBlock Grid.Row="6"
                           Text="Подключение к StatHammer.Server"
                           Foreground="#6B7280"
                           VerticalAlignment="Bottom"
                           Margin="0,18,0,0" />
            </Grid>
        </Border>
    </Grid>
</Window>